# OS files
.DS_Store
Thumbs.db

# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Python virtual environments and cache
.venv/
venv/
env/
ENV/
pyvenv.cfg
.python-version
__pycache__/
*.py[cod]
*$py.class

# Next.js and frontend build artifacts
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
next-env.d.ts

# Logs
*.log
*.log.gz
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
daemon.log
daemon_output.log

# Environment and sensitive files
.env
.env.local
.env.*.local
.env.production
.env.staging
.env.development
.env.sample
!.env.example
!.env.production.example
*.pem
*.key

# IDE settings
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Vercel
.vercel

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/
tests/__artifacts__/
apps/*/tests/__artifacts__/

# Miscellaneous
.npm
.eslintcache

# Claude local settings — contains auto-allowed commands that may include secrets
.claude/settings.json

# =====================================================
# Dados locais de runtime (NUNCA subir)
# =====================================================

# Test cases salvos localmente (fallback quando Supabase offline)
data/

# Runs locais de Maestro (output de execucao)
flows/runs/

# YAMLs gravados / em rascunho locais — tudo que termina em padroes comuns de teste local.
# Se algum YAML for canonico do projeto, comite explicitamente com git add -f.
flows/**/Novo_*.yaml
flows/**/Gravando*.yaml
flows/**/Gravado*.yaml
flows/**/*Recorder*.yaml
flows/**/*Gracvado*.yaml

# Maestro Studio frontend (extraido do .asar + patchado para localhost:8001/mss).
# VERSIONADO de proposito: e um artefato pre-buildado sem step de build neste
# repo, e precisa ir junto na imagem Docker do web — senao /maestro-studio/
# da 404 em producao (so existiria na maquina de quem extraiu localmente).

# Maestro CLI clonado para referencia (~100MB) — fica local, nao em git
Maestro/

# Artefatos de runtime do daemon
apps/daemon/value.mp4
apps/daemon/daemon.log
apps/daemon/daemon_output.log
apps/daemon/logs/

# Logs gerais
logs/

# Build artifacts adicionais
**/.tsbuildinfo

README.md
README.md
